# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 Production Environment Variables
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# HK Legal Case Agency - DigitalOcean 部署配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📊 DATABASE (必需)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# DigitalOcean Managed Database 連接字串
# 格式: postgresql://[用戶名]:[密碼]@[主機]:[端口]/[資料庫名]?參數
# 
# 📝 重要參數說明:
# - connection_limit=5      → 每個應用實例最多使用 5 個連接
# - pool_timeout=10         → 連接池等待時間 10 秒
# - connect_timeout=10      → 連接超時 10 秒
# - sslmode=require         → DigitalOcean Managed DB 要求 SSL
# - schema=public           → 使用 public schema
#
# 💡 為什麼 connection_limit=5？
#    DigitalOcean 基本方案限制 47 個同時連接
#    單實例 5 個連接非常充足，預留空間給：
#    - 多實例擴展（如自動擴展到 3 實例 = 15 個連接）
#    - 管理工具和監控服務
#    - 手動資料庫操作
#
# 🔍 從 DigitalOcean 取得連接字串：
#    1. 進入 Databases → 選擇您的資料庫
#    2. 點擊 "Connection Details"
#    3. 選擇 "Connection String" 或 "Connection Parameters"
#    4. 在連接字串後面添加以下參數

DATABASE_URL="postgresql://doadmin:AVNS_xxxxx@db-postgresql-sgp1-12345-do-user-12345.b.db.ondigitalocean.com:25060/defaultdb?sslmode=require&connection_limit=5&pool_timeout=10&connect_timeout=10&schema=public"

# 🎯 其他資料庫選項（如果不使用 DigitalOcean Managed Database）:
# Supabase: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres?connection_limit=5&pool_timeout=10
# Railway: postgresql://postgres:[password]@[region].railway.app:5432/railway?connection_limit=5&pool_timeout=10
# AWS RDS: postgresql://[user]:[password]@[endpoint].rds.amazonaws.com:5432/[dbname]?connection_limit=5&pool_timeout=10

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 AUTHENTICATION (必需)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# NextAuth 密鑰（用於簽署 session token）
# 🔑 生成方式:
#    方法 1: openssl rand -base64 32
#    方法 2: https://generate-secret.vercel.app/32
#    方法 3: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-generated-secret-here"

# 應用程式完整 URL（包含 https://）
# ⚠️ 必須與您的域名完全一致，不要加尾隨斜線
NEXTAUTH_URL="https://your-domain.com"

# 公開應用程式 URL（前端使用）
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🌐 GOOGLE OAUTH (可選 - 如果啟用 Google 登入)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Google OAuth 憑證
# 📝 取得方式: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📧 EMAIL (可選 - 如果啟用郵件通知)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# SMTP 配置
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# SMTP_FROM="noreply@your-domain.com"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔧 APPLICATION SETTINGS (可選 - 使用預設值即可)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Node 環境（DigitalOcean 會自動設定）
NODE_ENV="production"

# Next.js 遙測（建議關閉以提升效能）
NEXT_TELEMETRY_DISABLED="1"

# 國際化預設語言
# NEXT_PUBLIC_DEFAULT_LOCALE="zh"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📌 DigitalOcean App Platform 設定步驟
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1️⃣ 進入您的 App → Settings → Components → web
# 2️⃣ 找到 "Environment Variables" 區域
# 3️⃣ 點擊 "Edit" 或 "Add Environment Variable"
# 4️⃣ 逐一添加以下必需變數:
#    ✅ DATABASE_URL (從 Databases → Connection Details 複製)
#    ✅ NEXTAUTH_SECRET (使用 openssl rand -base64 32 生成)
#    ✅ NEXTAUTH_URL (您的域名，如 https://looperhq.hk)
#    ✅ NEXT_PUBLIC_APP_URL (與 NEXTAUTH_URL 相同)
# 5️⃣ 儲存並重新部署
#
# 🔍 驗證連接:
#    部署後檢查 "Runtime Logs"，應該看到:
#    ✅ "Prisma schema loaded"
#    ✅ "Database connected"
#    ✅ "Server listening on port 8080"
#    ❌ "connection refused" → DATABASE_URL 錯誤
#    ❌ "too many connections" → 需要調整 connection_limit
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
